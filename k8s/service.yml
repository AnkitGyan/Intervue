apiVersion: v1
kind: Service

metadata:
  name: intervue

spec:
  type: NodePort

  selector:
    app: intervue

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000